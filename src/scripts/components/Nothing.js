'use strict';

var React = require('react/addons');

require('../../styles/Nothing.less');

var Nothing = React.createClass({
  render: function () {
    return (
        <div>
          <p>Content for Nothing</p>
        </div>
      );
  }
});

module.exports = Nothing; 

