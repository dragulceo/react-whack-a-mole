'use strict';

var React = require('react/addons');

require('../../styles/Control.less');

var Control = React.createClass({
  render: function () {
    return (
        <div>
          <p>Content for Control</p>
        </div>
      );
  }
});

module.exports = Control; 

